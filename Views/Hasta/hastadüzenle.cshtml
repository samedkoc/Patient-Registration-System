@using Samed_Koç_Fİnal.Models
@model Hasta

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
                <div class="card-header bg-warning text-dark p-4 border-0 text-center">
                    <h4 class="mb-0 fw-bold"><i class="bi bi-pencil-square me-2"></i>Hasta Düzenle</h4>
                </div>
                <div class="card-body p-4">

                    @if (TempData["Uyarı Mesajı"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show shadow-sm border-0 rounded-3 mb-4" role="alert">
                            <i class="bi bi-exclamation-triangle-fill me-2"></i> @TempData["Uyarı Mesajı"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form asp-action="hastadüzenle" method="post">
                        <input type="hidden" asp-for="Id" />

                        <div class="form-floating mb-3">
                            <input type="text" class="form-control rounded-3" id="AdSoyad" asp-for="AdSoyad" placeholder="Ad Soyad" required />
                            <label for="AdSoyad">Ad Soyad</label>
                        </div>
                        <div class="form-floating mb-3">
                            <input type="text" class="form-control rounded-3" id="TCKimlikNo" asp-for="TCKimlikNo" placeholder="TC Kimlik No" required />
                            <label for="TCKimlikNo">TC Kimlik No</label>
                        </div>
                        <div class="form-floating mb-4">
                            <input type="number" class="form-control rounded-3" id="Yas" asp-for="Yas" placeholder="Yaş" required />
                            <label for="Yas">Yaş</label>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a asp-action="Index" class="btn btn-outline-secondary rounded-pill px-4">İptal</a>
                            <button type="submit" class="btn btn-warning rounded-pill px-4 shadow-sm text-dark">
                                <i class="bi bi-arrow-clockwise me-1"></i> Güncelle
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>